stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - vultr_group
  only:
    - main
  script:
    - cp -r --exclude=.git . /home/gitlab-runner/homyak
    - cd /home/gitlab-runner/homyak
    - docker compose up -d --build